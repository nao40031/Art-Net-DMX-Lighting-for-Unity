# AGENTS.md

このファイルは、このUnityプロジェクトでCodex/AIエージェントが作業するときの基本ルールです。
リポジトリ直下で作業する場合は、以後この内容を優先して参照してください。

## 基本方針

- Unity Inspector、EditorWindow、ログなどのテキストボックスは、原則として日本語の文章をまとめ、その後に空行を1行挟んで英語の文章をまとめて記載する。文ごとに日本語と英語を交互に並べない。対応する英語が不要な固有名詞・コード値は例外とする。

- まず現在の状態を確認し、推測だけで実装・削除・移動を進めない。
- ユーザーが「調査」「確認」「分析」と言っている場合は、原則として読み取り専用で進める。
- 既存のUnity設定、フォルダ構成、命名、実装パターンを優先する。
- 他プロジェクトへ移植しても壊れにくい実装を選ぶ。
- やりたいことを100%実現するプランをまず検討し、その上で処理負荷を抑える代替案も提示する。
- 軽量化を優先するあまり、本来再現できる機能や品質を不用意に制限しない。
- 重い処理、常時Update、全件検索、大量Instantiate、同期I/O、巨大アセット読み込みは、必要性と代替手段を比較して判断する。
- Git、Git LFS、Unityメタファイル、シーン参照、Timeline参照、Prefab参照を不用意に壊さない。

## 実装前の進め方

実装に入る前に、必ず実装プランを提示し、ユーザーの承認を待ってください。

実装プランには次を含めます。

1. 変更対象のファイル・機能
2. 実装内容の概要
3. 想定される影響範囲
4. 検証方法
5. 通常版の実装プラン
6. 軽量版の実装プラン

ユーザーが明示的に「そのまま実装して」「承認不要」と言った場合を除き、承認前にコード編集を行わないでください。
ただし、状態確認のための読み取り、検索、`git status` などの非破壊コマンドは実行して構いません。

## 軽量版プランの考え方

通常版のプランとは別に、処理負荷・実装量・リスクを抑えた軽量版を提示してください。
通常版は、処理負荷が重くなる可能性があっても、ユーザーがやりたいことを100%実現する案として検討します。
軽量版は、60〜80%程度の再現度・品質でも実用上問題が少ない場合に、負荷を下げる代替案として提示します。

軽量版を提示するときは、次を明記してください。

- 通常版で実現できること
- 軽量版で維持できること
- 軽量版で省略・簡略化されること
- 想定される品質・再現度の差
- 処理負荷や実装リスクがどの程度下がるか

軽量版では、次の観点を優先します。

- 毎フレーム処理を避け、イベント駆動・キャッシュ・差分更新を使う。
- 反射、Find系API、全オブジェクト走査、Resources全読み込みを避ける。
- 初期化時に一度だけ参照を解決し、実行中は再利用する。
- 必要な範囲だけを更新し、変更がないときは処理しない。
- 既存コンポーネントへの最小変更で実現する。
- 機能を段階的に足せる構造にする。

## 実装時のルール

- 実装に入る前に承認済みプランと差分がないか確認する。
- 差分が出た場合は、勝手に広げずにユーザーへ確認する。
- 変更は依頼範囲に絞り、不要なリファクタや整形変更を混ぜない。
- Unityの `.meta` ファイルは対応するアセットと一緒に扱う。
- シーン、Prefab、Timeline、Animator、ScriptableObjectへの参照はGUID検索やYAML確認で確認する。
- 他プロジェクトでも使う可能性があるコードは、プロジェクト固有の絶対パス、固定シーン名、固定GameObject名への依存を避ける。
- 必要な依存関係は明示し、存在しない場合でもエラーで止まりにくいガードを入れる。
- エディタ拡張やランタイム処理は、Unityのバージョン差異を考慮して保守的に実装する。

## 使い方の提示

実装した場合は、完了報告と一緒に使い方を提示してください。

使い方には必要に応じて次を含めます。

- Unity Editor上での設定手順
- Inspectorで設定する項目
- 実行・確認手順
- 想定される入力やファイル配置
- トラブル時に確認するログや設定
- 他プロジェクトへ移植する場合の注意点

## 検証

変更後は、可能な範囲で検証してください。

- C#変更時は、コンパイルエラーが起きにくいか構文・API使用を確認する。
- Unityプロジェクト設定やアセット変更時は、参照切れや `.meta` の欠落に注意する。
- Git関連作業では、`git status` と必要に応じてGit LFS状態を確認する。
- 実行検証できない場合は、できなかった理由と残るリスクを明記する。

## GitとGit LFS

- 大きなバイナリやUnityアセットは、必要に応じてGit LFS対象か確認する。
- LFS対象を増減させる前に、対象ファイルのサイズ、参照状況、GitHub制限への影響を確認する。
- `Library/`、`Temp/`、`Logs/`、`obj/`、ユーザー固有設定などの生成物は原則として追跡しない。
- 既存の履歴、バックアップ、リモート設定を壊す操作は、事前に明示して承認を取る。

## 報告形式

最終報告では、簡潔に次を伝えてください。

- 変更した内容
- 変更したファイル
- 実施した検証
- 使い方
- 未検証事項や残リスク

作業途中で判断が必要になった場合は、実装を進める前にユーザーへ確認してください。
